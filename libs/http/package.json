{
  "name": "@macavitymadcap/hyper-dank-transport",
  "version": "0.1.0",
  "type": "module",
  "description": "Small Hono and HTMX helpers for hypermedia-first apps.",
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./src/index.ts"
    }
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "pack:local": "bun pm pack --destination ../../.cache/packages"
  },
  "peerDependencies": {
    "hono": "^4.0.0",
    "typescript": "^5"
  }
}

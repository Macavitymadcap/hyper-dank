# pace-0028: Enable GitHub Pages Site Setup

## Header

| Field | Value |
| --- | --- |
| Epic | `pace-0028` |
| Branch | `pace-0028` |
| Status | Implemented |
| Theme | GitHub Pages deployment enablement |
| Ticket Branches | N/A |
| Owner | `@Macavitymadcap` |
| Target PR | TBD |
| Last updated | 2026-05-18 |

## Summary

Allow the Pages deployment workflow to initialise GitHub Pages when the repository does not already
have a Pages site configured.

## Goals

- Pass `enablement: true` to `actions/configure-pages@v5`.
- Keep the existing Jekyll, static demo, Storybook, and artifact deployment flow unchanged.
- Restore the `build-pages` GitHub Actions gate after `pace-0020` lands.

## Non-Goals

- No changes to the generated Pages artifact.
- No repository rename.
- No deployment workflow replacement beyond the setup input.

## Verification

- `bun run check`
- `bun run verify` passed on `main` before this workflow-only patch.

## Acceptance Criteria

- The branch-flow gate accepts the PR into `main`.
- The Pages setup step can enable the repository Pages site when needed.
- Existing local checks remain green.

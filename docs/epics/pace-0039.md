# pace-0039: Restore Walk History Table Layout

## Header

| Field | Value |
| --- | --- |
| Epic | `pace-0039` |
| Branch | `pace-0039` |
| Status | Implemented |
| Theme | Static demo hotfix |
| Ticket Branches | None |
| Owner | `@Macavitymadcap` |
| Target PR | [#34](https://github.com/Macavitymadcap/pace-calculator/pull/34) |
| Last updated | 2026-05-18 |

## Summary

Restore the static walking pace demo's walk history table layout after the `pace-0031` epic was
squash-merged into `main` without the later table-fix commit from the epic branch.

## Goals

- Restore semantic table layout in the static demo walk history.
- Keep the static demo smoke coverage asserting the table structure.
- Land the fix on `main` before starting implementation ticket `pace-0032`.

## Non-Goals

- No broader platform-epic implementation work from `pace-0031`.
- No changes to shared package APIs or release automation.
- No follow-on ticket branches under this hotfix epic.

## Ticket Plan

| Ticket | Branch | Scope | Acceptance Criteria |
| --- | --- | --- | --- |
| None | N/A | Hotfix lands directly on the epic branch | PR merges with `bun run verify` passing |

## Architecture Notes

The fix should stay limited to the static demo rendering path and its smoke coverage. It should not
pull broader `pace-0031` planning or implementation changes onto `main`.

## Integration Plan

- Open this epic branch as a pull request into `main`.
- Merge the hotfix directly after verification passes.
- Start `pace-0032` from updated `main` after this hotfix lands.

## Verification

- `bun run verify`

## Assumptions

- `pace-0039` is the next available epic branch number.
- The hotfix should land independently of the larger `pace-0031` plan.

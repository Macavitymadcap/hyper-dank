# pace-0024: Rename Public Repository Identity To Hyper-Dank

## Header

| Field | Value |
| --- | --- |
| Ticket | `pace-0024` |
| Branch | `pace-0024` |
| Parent Epic | `pace-0020` |
| Base Branch | `pace-0020` |
| Status | Implemented |
| Theme | Repository identity and public naming |
| Milestones | 1 implementation slice |
| Primary stack | Markdown, GitHub scripts, repository metadata |
| Owner | `@Macavitymadcap` |
| Target PR | TBD |
| Last updated | 2026-05-18 |

## Summary

Update the active repository identity from Pace Calculator to Hyper-Dank while preserving historical
PR links and the `pace-*` branch numbering convention.

## Goals

- Point active docs and script defaults at `Macavitymadcap/hyper-dank`.
- Make README and architecture language introduce Hyper-Dank first, with Walking Pace as the demo.
- Keep package names unchanged.
- Record the manual post-merge GitHub repository rename.

## Non-Goals

- No full folder rename for `apps/walking-pace`.
- No package-name change for the Hyper-Dank libraries.
- No rewrite of historical PR links in prior roadmap docs.

## Implementation

- Update active docs, branch-protection notes, and public setup language.
- Change hard-coded default repo values in helper scripts from `Macavitymadcap/pace-calculator` to
  `Macavitymadcap/hyper-dank`.
- Keep `pace` as the branch prefix until a separate workflow migration is explicitly planned.

## Tests First

- Run `bun run check`.
- Run the branch-flow tests implicitly through CI on the epic PR.

## Acceptance Criteria

- New contributors see Hyper-Dank as the repository identity.
- The manual GitHub rename action is documented.
- Existing release and branch-flow expectations remain discoverable.

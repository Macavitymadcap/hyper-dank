export const walksCellStyles = /* css */`
.walks-cell {
  text-align: center;
  padding: var(--size-2);
  color: var(--table-text);
}

@media (max-width: 480px) {
  .walks-cell {
    padding: var(--size-1);
  }
}
`;
